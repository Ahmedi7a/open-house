<%- include('../partials/head') %> <br>
    <div class="container">
        <h1>Details</h1>
   
                <div class="card mt-2" style="width: 18rem;">
                    <img src="https://images.pexels.com/photos/106399/pexels-photo-106399.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1" class="card-img-top" alt="...">
                    <div class="card-body">
                      <h5 class="card-title">Price: $<%= listing.price %></h5>
                      <p class="card-text"><%= listing.streetAddress %></p>
                      <p class="card-text"><%= listing.size %> m/sq</p>
                      <p class="card-text">Owner: <%= listing.owner.username || 'Unkown' %> </p>
                      <a href="/listings" class="btn btn-primary">Go Back</a>
                      <% if (listing.owner._id.equals(user._id)) { %>
                        <form action="/listings/<%= listing.owner._id %>/<%= listing._id %>?_method=DELETE" method="POST">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                     <% } %>
                    </div>
                  </div>
    </div>

<%- include('../partials/footer') %>